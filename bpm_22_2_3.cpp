#include <iostream>
#include <string>

int main(){
    int n = 0;
    std::string str;
    std::string ostr;
    std::cin >> n >> str;
    for (int i = n-1;i>0;i-=1){
        ostr.insert(ostr.size()/2,);
        
    }
    //volga voga vga va a 
    //logva logv log lo l 
}